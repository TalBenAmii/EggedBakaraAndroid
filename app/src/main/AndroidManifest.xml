<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="app.example.bubithebakar">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.BubiTheBakar">
        <meta-data
            android:name="com.google.android.actions"
            android:resource="@xml/progress" />

        <activity android:name="app.example.bubithebakar.LoginScreen" />
        <activity android:name="app.example.bubithebakar.LeaderBoard" />
        <activity android:name="app.example.bubithebakar.DataHistory" />
        <activity android:name="app.example.bubithebakar.Data" />
        <activity android:name="app.example.bubithebakar.LoadingScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>